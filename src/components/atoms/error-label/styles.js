import {StyleSheet} from 'react-native';
import colors from '../../../assets/colors';

export default StyleSheet.create({
  error: {
    color: colors.tint,
    fontSize: 12,
    textAlign: 'right',
    marginTop: 6,
    fontWeight: '600',
  },
});
