import {StyleSheet} from 'react-native';
import colors from '../../../assets/colors';

export default StyleSheet.create({
  image: {
    width: '100%',
    height: 220,
  },
  title: {
    color: colors.tint,
    fontSize: 18,
    fontWeight: '500',
  },
});
